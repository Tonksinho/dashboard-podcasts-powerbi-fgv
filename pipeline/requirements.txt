# SpotiScript - Dependências
# Instale com: pip install -r requirements.txt

scrapling[fetchers]>=0.2.99
python-dotenv>=1.0.0
gspread>=5.12.0
oauth2client>=4.1.3

# requests mantido apenas para spotiscrap_legacy.py
requests>=2.31.0

# Scrapling usa patchright/playwright internamente para StealthySession
playwright>=1.44.0

# Nota: oauth2client está deprecated, mas mantido por compatibilidade com o código atual.
# Futuramente pode ser migrado para google-auth.